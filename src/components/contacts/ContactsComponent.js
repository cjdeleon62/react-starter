import React from 'react';

const ContactsComponent = () => (
  <section>
    This is the ContactsComponent
  </section>
);

ContactsComponent.displayName = 'ContactsComponent';
export default ContactsComponent;
